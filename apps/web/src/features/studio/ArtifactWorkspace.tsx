import { StoryMap } from "./StoryMap.js"; import { PlayPreview } from "../play/PlayPreview.js"; import { FindingsPanel } from "../playtest/FindingsPanel.js";
export function ArtifactWorkspace({ view }: { view: string }) { if (view === "Story map") return <StoryMap />; if (view === "Play preview") return <PlayPreview />; if (view === "Findings") return <FindingsPanel />; return <section><h2>{view}</h2><p className="warning">This artifact is stale after the latest source edit.</p><article><h3>The orchard gate</h3><p>You stand before a silver gate. A promise made here changes what opens beyond it.</p></article></section>; }
