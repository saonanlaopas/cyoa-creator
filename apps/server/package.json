{
  "name": "@story-to-cyoa/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/multipart": "^9.2.1",
    "@fastify/static": "^8.2.0",
    "@story-to-cyoa/importers": "workspace:*",
    "@story-to-cyoa/domain": "workspace:*",
    "@story-to-cyoa/export-twine": "workspace:*",
    "@story-to-cyoa/openrouter": "workspace:*",
    "@story-to-cyoa/pipeline": "workspace:*",
    "@story-to-cyoa/persistence": "workspace:*",
    "fastify": "^5.5.0"
  },
  "devDependencies": {
    "tsx": "^4.20.3"
  }
}
