import type { StoryBible } from "../schemas/story-bible.js"; import { AdaptationPlanSchema, type AdaptationPlan } from "../schemas/adaptation-plan.js";
export function proposeAdaptation(bible: StoryBible, overrides: Partial<AdaptationPlan> = {}): AdaptationPlan { const protagonist = bible.characters[0] ?? "the existing protagonist"; return AdaptationPlanSchema.parse({ protagonist, visibleStats: [{ key: "resolve", label: "Resolve" }, { key: "insight", label: "Insight" }, { key: "trust", label: "Trust" }], relationships: [{ key: "ally", label: "Ally relationship" }], contentBoundaries: ["preserve source sensitivities"], branchBudget: 8, divergencePoints: ["first difficult choice"], reconvergencePoints: ["late-act reckoning"], endings: ["success", "partial", "failure"], ...overrides }); }
export function approveAdaptation(plan: AdaptationPlan): AdaptationPlan { return { ...plan, approved: true }; }
