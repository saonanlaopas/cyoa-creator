export function ImportStep({ chapters, select }: { chapters: string[]; select: (chapter: string) => void }) { return <section><h2>Import story</h2><label>Source file <input type="file" accept=".txt,.md,.docx" /></label><p>Select chapters to adapt:</p>{chapters.map((chapter) => <label key={chapter}><input type="checkbox" defaultChecked onChange={() => select(chapter)} /> {chapter}</label>)}</section>; }
