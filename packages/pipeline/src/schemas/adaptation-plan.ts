import { z } from "zod";
export const AdaptationPlanSchema = z.object({ version: z.number().int().positive().default(1), protagonist: z.string(), divergenceMode: z.enum(["canon-centered", "balanced", "expansive"]).default("balanced"), randomness: z.boolean().default(false), visibleStats: z.array(z.object({ key: z.string(), label: z.string() })).min(3).max(5), relationships: z.array(z.object({ key: z.string(), label: z.string() })), contentBoundaries: z.array(z.string()), branchBudget: z.number().int().min(3), difficulty: z.enum(["gentle", "standard", "hard"]).default("standard"), divergencePoints: z.array(z.string()), reconvergencePoints: z.array(z.string()), endings: z.array(z.string()).min(3).max(5), approved: z.boolean().default(false) });
export type AdaptationPlan = z.infer<typeof AdaptationPlanSchema>;
