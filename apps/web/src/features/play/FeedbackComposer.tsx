import { useState } from "react"; export function FeedbackComposer({ passageId = "gate" }: { passageId?: string }) { const [sent, setSent] = useState(false); return <form onSubmit={(e) => { e.preventDefault(); setSent(true); }}><label>Feedback for passage {passageId}<textarea name="feedback" /></label><button>Send feedback</button>{sent && <span role="status">Attached to {passageId}</span>}</form>; }
